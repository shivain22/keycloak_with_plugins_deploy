# Example environment variables for Jenkins pipeline creation
# Copy this file and update with your values, then source it before running scripts

# Jenkins Connection
JENKINS_URL=http://localhost:8080
JENKINS_USERNAME=admin
JENKINS_PASSWORD=your-api-token-here

# Job Configuration
JOB_NAME=Keycloak-Deployment
GIT_REPO_URL=https://github.com/your-username/keycloak_with_plugins_deploy.git
GIT_BRANCH=*/master
JENKINSFILE_PATH=Jenkinsfile

# Optional: Git Credentials (if repository is private)
CREDENTIALS_ID=github-credentials

# Optional: Config XML path
CONFIG_XML_PATH=jenkins-job-config.xml

